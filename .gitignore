.env
node_modules
.next/
test-results/
playwright-report/
*.tsbuildinfo
*.log

docs/research/reddit-demand-analysis.md

# Agent persona harness output: reports + screenshots of REAL plans made
# against the deployed app, plus a saved signed-in session that is a real
# credential. Point-in-time artifacts of real data; never committed.
testing/agent-personas/output/
