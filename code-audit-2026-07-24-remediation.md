# Code audit remediation — 2026-07-24

This is the live tracker for the 2026-07-24 full-project audit. Every finding is verified
against the current repository before implementation. Historical `DEVLOG.md` entries and the
2026-07-18 audit are evidence, not assumed current truth.

## Baseline

- Branch: `main`
- Starting commit: `6f36584ab29e9eb10d69a3556a13db9c673e3ee9`
- Starting untracked paths: `AGENTS.md` only (user-owned, relevant, preserved)
- Node: `v24.16.0`
- npm: `11.13.0`
- TypeScript: pass (`npx tsc --noEmit`)
- Production build: pass (Next.js 14.2.35)
- Unit tests: 251/251 across 18 suites
- Mock E2E: 41/41 on port 3100
- Lint: unusable; `next lint` opened an interactive configuration prompt
- Audit: failed with 2 high-severity dependency findings (Next.js and PostCSS)
- Port 3000: untouched

## Re-baseline — 2026-08-01 after Claude feature streams

- Branch: `main`
- Current local commit before this documentation pass: `04cc7401e44d5cb7c29a43b48178f1a9da1caeb2`
- Fetched `origin/main`: `04cc7401e44d5cb7c29a43b48178f1a9da1caeb2` (local `main` matched exactly; no pull was needed)
- Starting working tree: clean; no untracked files, merge/rebase state, or local-only commits
- Branches: local `main` and `authentication`; remote `origin/main` and `origin/authentication`. Local `authentication` points at `04cc740`; `origin/authentication` points at Stage 1A commit `af511a6`. No Stage 1B branch is present locally or remotely.
- Node: `v24.18.1`
- npm: `11.16.0`
- Next.js: `16.2.11`
- React / React DOM: `19.2.8` / `19.2.8`
- TypeScript: `5.9.3`; `npm run typecheck` passed
- Deterministic install: `npm ci` passed (445 packages installed; lockfile unchanged)
- Lint: passed with 0 errors and 11 warnings. The live warnings are 3 unused disable directives plus 8 unused variable/parameter warnings in `ItineraryMap.tsx`, `store.ts`, `swap.test.ts`, and `page.tsx`.
- Unit tests: 459/459 across 36 suites via the aggregate `npm run test:unit` runner. The first sandboxed `tsx` startup failed before loading a suite because Node's `uv_os_get_passwd` returned `ENOMEM`; the same command passed outside that constrained process environment.
- Production build: passed with Next.js 16.2.11 (Turbopack); TypeScript and all 10 static-page generation steps completed.
- Mock E2E: **71/73 passed across 10 spec files; aggregate gate failed.** The two deterministic failures are the stated-window scenarios in `e2e/scenarios.spec.ts`: `dinner and drinks from 5-9pm` and the over-stuffed `dinner and drinks and dessert from 7-9pm` both fail loud with “Couldn't fit anything into your … window once travel time is counted”. A focused rerun reproduced both. This session is documentation-only, so no production or test fix was attempted.
- Aggregate `npm run check`: failed at its final mock-E2E stage because of the two failures above; lint, typecheck, unit, and build passed when run in the same baseline investigation.
- Mock-network boundary: no external browser requests were allowed (`e2e/test.ts` aborts every non-local HTTP(S) request unless live mode explicitly opts out), and mock fixtures replaced Groq, Geocoding, Places, Routes, and Weather provider calls. No external provider request was made by mock E2E.
- `npm audit`: 1 high-severity development-tooling advisory (`brace-expansion` below 1.1.17 through ESLint dependency paths); dependencies were not changed.
- `npm audit --omit=dev`: 0 vulnerabilities.
- Port 3000: untouched. Mock E2E used its isolated server on port 3100.

## Finding tracker

`SELF` in the commit column means the row was updated in the same commit as the change; exact
hashes are reconciled after the commit exists.

| Finding ID | Verified status | Root cause | Files changed | Tests added | Revert-run performed | Commit hash | Remaining manual work | Notes or deviations |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| H1 | Confirmed — source mitigated; external global limit required | Every public route accepted unbounded request bodies and unlimited bursts before doing provider or persistence work. | `app/api/_shared/http.ts`; all public route handlers; `DEPLOY.md` | 4 boundary cases cover declared/actual oversize rejection and rate-limit ordering ahead of parse-provider work. | No | SELF | Configure an edge-, Redis-, or platform-backed global limiter; set provider quotas/billing alerts; verify every production key restriction. The in-process limiter remains defense in depth, not a global serverless quota. | **Reconfirmed 2026-08-01:** `REQUEST_LIMITS`, bounded client keys/bucket cleanup, and per-scope in-process buckets remain. Routes invoke the boundary before provider or persistence work; oversize and rate-limited parse requests make zero mocked provider calls, and 429 includes `Retry-After`. Focused shared HTTP/schema/provider result: 12/12. |
| H2 | Confirmed — fixed | Next 14.2.35 and its bundled PostCSS/Sharp versions were covered by current high-severity advisories. | `package.json`; `package-lock.json`; `next.config.mjs`; `tsconfig.json`; `next-env.d.ts`; three dynamic itinerary routes | Existing 251 unit and 41 mock E2E tests exercised after the migration. | No | SELF | Full audit still reports dev-only transitive `brace-expansion`/`minimatch` advisories under ESLint; production audit is clean. | Upgraded to Next 16.2.11/React 19.2.8, migrated async route params, and pinned patched PostCSS/Sharp through Next-scoped overrides. |
| H3 | Confirmed — fixed | Redis and memory routes performed mutable load/modify/save cycles with no version check; GET lock ratcheting could overwrite a newer swap/reroute and every Redis save reset the TTL. | `app/api/itinerary/store.ts`; create/read/swap/reroute routes; client mutation bodies; shared version schema | 9 new cases: two swaps, swap versus reroute, lock ratchet versus swap, stale writer 409, failed-CAS isolation, no-op version stability, route ETag/conflict ordering, and Redis atomic TTL preservation. | Yes — removing the in-memory version comparison fails 4/6 CAS cases with lost edits, missing retries, and no stale rejection; restored result is 6/6. | SELF | — | New plans start at version 1; Redis CAS is one Lua `EVAL` using `SET ... KEEPTTL`; memory mode clones and CASes; legacy unversioned Redis records normalize to version 1; explicit stale clients fail before provider work while safe versionless/status operations retry at most three times. |
| H4 | Confirmed — fixed | Reroute compacted selections with IDs/locations for routing, then scheduled and wrote the uncompacted slot list; missing/malformed selections could silently shorten the route or attach an A→C leg to A→B, and the engine mutated the caller before proving completeness. | `app/api/itinerary/reroute.ts`; reroute route; atomic/route tests | Empty/missing selections, missing middle coordinates, A→B→C alignment, provider failure, partial-failure byte identity, and failed-route version stability. | Yes — accepting a missing location makes the middle-coordinate case alone fail (11/12) by committing the malformed chain; restored result is 12/12. | SELF | — | The engine clones first, requires exactly one valid unique in-pool ID and coordinate pair per affected slot, rejects gaps/compaction, verifies schedule/arrival agreement, and copies back once only after the complete chain passes. |
| H5 | Confirmed — fixed | Every replacement was filtered at one floor/anchor instant and every downstream route request reused that same departure, so later openings, closings, weather, and transit timing were judged at the wrong time. | `app/api/itinerary/reroute.ts`; arrival-aware reroute tests | Accumulated departure times, later opening, closes-before-arrival replacement, slot-varying weather, longer-leg closing crossover, bounded exhaustion, and one-fetch pool/weather assertions. | Yes — forcing all validation back to the anchor fails 6/12 cases, including later opening, closing, weather, long-leg, exhaustion, and atomic-failure scenarios; restored result is 12/12. | SELF | — | Pools and forecast are fetched once; up to five stabilization passes route sequentially, validate each candidate through the shared objective filter at its settled arrival, exclude invalid IDs, and reroute the full tentative chain. |
| H6 | Confirmed — fixed | Venue swaps wrote the target before proving its new inbound leg could reach the held start, and every swap treated the first locked tail stop as a successful stopping point even when the new prefix overlapped it. | `app/api/itinerary/swap.ts`; swap route; legacy and atomic swap tests | Farther fixed-slot venue, valid moved target, locked collision, inconsistent/reachable locked boundaries, production-mock seam, and failed-swap version/no-mutation coverage. | Yes — restoring the silent missing-tail `continue` made the missing-middle case fail by reporting a successful partial proposal (10/11 at the time of the revert); the restored current suite is 12/12. | SELF | — | Venue swaps keep the complete existing slot and now refuse an unreachable replacement; time swaps may move the target; duration swaps keep its start. Every path routes the anchor inbound before write-back and proves any locked boundary is reachable without overlap. |
| H7 | Confirmed — fixed | `resettleTail` returned success after `break` on missing coordinates or a locked boundary, checked only hours before shifting, fetched weather repeatedly, and did not revalidate an adapted venue after its new inbound leg changed arrival. | `app/api/itinerary/swap.ts`; swap route; `swap.atomic.test.ts`; corrected locked-boundary expectations in `swap.test.ts` | Shifted closing/weather cases, missing middle location, replacement closing crossover, one-weather-fetch assertion, route failure byte identity, and complete reachable-boundary success. | Yes — the H6/H7 shared revert-run above directly resurrected the partial-tail success bug and was caught by the atomic suite. | SELF | — | Every swap is built on a clone with one forecast snapshot. The tail is routed sequentially; existing and adapted stops pass the shared objective filter plus the availability seam at final arrival; replacement-route changes trigger another validation; only a complete proposal is copied back, while refusal is a persistence no-op. |
| H8 | Confirmed — fixed | The scheduler understood clocks and a special `tomorrow` branch but discarded weekday/date qualifiers, relied on permissive `Date` normalization, and had no deterministic DST policy. | `app/api/schedule/schedule.ts`; `app/lib/zoneTime.ts`; schedule/parse/mock tests; `README.md` | Bare/next weekdays, ISO/named dates, date-plus-clock, invalid dates, DST gap/overlap, and mock parse preservation. | Yes — removing bare-weekday resolution fails the calendar regression; restored focused batch result is 147/147. | SELF | — | Calendar math stays in `zoneTime.ts`; bare weekdays use the documented nearest-future rule, `next` is strict when today matches, DST gaps fail, and overlaps select the earlier instant. **Reverified 2026-08-01:** the legacy/fallback resolver remains fixed (43/43 schedule and 11/11 hours tests), while the planner replacement adds a raw-prompt weekday floor and coherent window shifting (35/35 planner tests). The planner does not preserve the old deterministic invalid-calendar/clock guard; see `H8/M3-F1`. |
| H8/M3-F1 | Confirmed — planner-path regression; implementation deferred | `planWithModel()` validates only the model's normalized `timeIntent`; it never applies the deterministic raw clock/calendar parser to the user's prompt. `applyTimeFloors()` repairs immediacy/all-day/weekday facts but not malformed syntax. A model can therefore normalize or ignore `13pm`, `00pm`, `24:00`, invalid minutes, signed lookalikes, or an impossible date and still pass. | Tracker only in this session; affected implementation is `app/api/parse/planner.ts` / parse route and guarding tests. | None — investigation/documentation-only change. Existing legacy schedule/hour tests pass but do not exercise the planner route's raw prompt. | Not applicable — investigation/documentation-only change. | SELF | Restore a deterministic pre-provider or pre-acceptance raw calendar/clock guard on the planner path and add route/planner regressions for every H8/M3 malformed case. | Read-only probe: a valid fixture response for `dinner at 13pm` was accepted with `source:"model"` and `startISO:"2026-08-01T13:00:00-04:00"`. First-activity plausibility itself is intentionally superseded: unusual hours now proceed to the objective opening-hours filter, and window feasibility is checked after real travel. |
| H9 | Confirmed — fixed | The selection prompt allowed general knowledge and prose/name inference as factual evidence, accepted unrelated unmet constraints, and could fall back to an unverified high-rated venue. | `app/lib/constraints.ts`; `app/api/select/selectVenues.ts`; select route/mock seam; fixture/filter/fail-loud tests | Normalization/inert-text cases, unrelated unmet values, unknown and valid accessibility, malformed retries, injection strings, multi-constraint reasons, and structured vegetarian E2E. | Yes — disabling code-side constraint enforcement makes the dedicated selection regressions fail; restored focused batch result is 147/147. | SELF | — | Only explicit provider booleans/accessibility fields become evidence. Unknown stays unknown; every selected ID and unmet constraint is revalidated after retry; constrained fallback uses evidenced candidates only. **Reverified 2026-08-01:** same deterministic mechanism survives the select/model rewrite. Focused results: constraints 4/4, select 28/28, model fallback 19/19. The fallback chain changes which validated model may answer, not the evidence boundary; corrections and constrained deterministic fallback remain code-revalidated. |
| M1 | Confirmed — fixed | `stop_count` was parsed but never converted into requested slots, so exact counts could silently collapse to one stop or bypass a missing-kind decision. | `app/lib/planSlots.ts`; `app/lib/clarify.ts`; parse/schema/schedule/select/client/mock code; unit and E2E tests | Three coffee shops, exactly three places, ambiguous dinner/drinks distribution, repeated categories, invalid counts, mandatory clarification, and three distinct strip/pin stops. | Yes — removing single-category expansion fails the repeated-count regression; restored focused batch result is 147/147. | SELF | — | Counts are bounded integers. One category expands exactly; multiple-category ambiguity asks for an explicit distribution; blank Go/Skip cannot bypass a counted request that still lacks a kind. **Reverified 2026-08-01:** the old `clarify.ts` mechanism is superseded; planner activities are now the count and `stop_count` is null. Legacy `planSlots` still passes 7/7, planner passes 35/35, select preserves repeated distinct slots (28/28), and six focused count/duplicate/recovery browser cases pass. The user guarantee is not deterministically checked against the raw prompt on the real planner path; see `M1-F1`. |
| M1-F1 | Confirmed — planner-path regression; implementation deferred | The planner prompt says a stated count wins, but `planWithModel()` / `validatePlan()` accept any non-empty activity list up to eight and never compare its length or distribution with the raw prompt. The mock planner hard-codes count expansion, so its green E2E does not guard a real model ignoring the count. Invalid/excessive raw counts can likewise disappear into a syntactically valid plan. | Tracker only in this session; affected implementation is planner validation/parse guarding plus tests. | None — investigation/documentation-only change. Existing exact-count mock E2E proves fixture plumbing only. | Not applicable — investigation/documentation-only change. | SELF | Reintroduce a deterministic raw count floor: reject/bound invalid counts, require exact activity cardinality, and require clarification for an ambiguous multi-category distribution before accepting model output. | Read-only probe: `planWithModel()` accepted one valid fallback-shaped activity for `exactly three places at 7pm` with `source:"model"`. This is a checkable fact delegated to the model and violates the proposer-only rule. |
| M2 | Confirmed — fixed | The objective filter removed an ID globally after it survived an earlier category, destroying feasible cross-category matchings before selection. The deterministic fallback also let later duplicate slots steal earlier top-ranked picks. | `app/api/places/search/filter.ts`; `app/api/select/selectVenues.ts`; filter/select/mock/browser tests | Dinner `[A,B]` plus drinks `[A]`, three overlapping categories, shared IDs across pools, repeated categories, narrowed shortages, and stable rating order. | Yes — restoring global pre-dedup fails the shared-ID filter/assignment cases; restored focused batch result is 147/147. | SELF | — | Deduplication is now per pool. Final IDs are globally unique through validated model output or maximum-cardinality augmenting assignment; free candidates are preferred before displacement to preserve request-order ranking. **Reverified 2026-08-01:** same mechanism after the select rewrite. Filter 24/24 and select 28/28 pass, including dinner `[A,B]` + drinks `[A]` → dinner `B`, drinks `A`, three-pool maximum-cardinality assignment, repeated slots, and stable early-slot preference. |
| M3 | Confirmed — fixed | Clock extraction accepted impossible meridiem/24-hour values (and signed lookalikes), malformed date-shaped text could be ignored, and plausibility passed when any later category allowed the hour. | `app/api/places/search/hours.ts`; `app/api/schedule/schedule.ts`; parse/schedule/hours tests | `13pm`, `00pm`, `24:00`, bad minutes, signed clocks, malformed dates, bare weekday/date 24-hour clocks, and first-category plausibility. | No — the strict parser and first-slot acceptance boundary are covered directly; no safe one-line revert represented both causes. | SELF | — | Clock parsing distinguishes absent from malformed input. Meridiem clocks require 1–12, bare clocks 0–23, minutes 0–59, and the first scheduled category owns plausibility. **Reverified 2026-08-01:** strict syntax remains fixed for legacy/fallback callers (43/43 schedule, 11/11 hours). The old plausibility table and time-gate were deliberately deleted; replacement behavior uses objective opening hours for each category and post-route window validation. Raw syntax is unguarded on the planner path; see `H8/M3-F1`. |
| M4 | Confirmed — fixed | Any budget containing a number was treated as “cheap,” so `under $300` and non-dollar amounts received the same hard Places cap as `under $20`. | `app/lib/budget.ts`; parse/filter/select/search/mock code; budget/filter/fixture tests | `$`, `$$`, cheap/budget language, `$20`, `$300`, prefix/suffix currencies, thousands separators, conflicting currencies, and missing price. | Yes — restoring a hard cheap cap for numeric maxima fails the numeric-budget filter case; restored focused batch result is 147/147. | SELF | — | Relative language and dollar-symbol levels retain defensible Places caps. Numeric maxima preserve amount/currency as ranking context only; missing/ambiguous price data remains eligible. |
| M5 | Confirmed — fixed | Recovery re-filtered every empty or weather-blocked category at the plan anchor, category names were used as slot identity, fresh pools replaced prior candidates, and occupied venue IDs were not excluded. | `app/page.tsx`; `app/lib/recoverySlots.ts`; `app/lib/planGuards.ts`; mock fixtures and recovery tests | 8 recovery-helper cases plus 3 browser scenarios cover later opening, later-slot weather, repeated categories, retained pools, and an occupied-only retry. | Yes — forcing every recovery target back to the plan anchor makes both later-slot browser scenarios fail (2/2); restored result is green. | SELF | — | Recovery rows carry stable numeric slots, provisional arrivals are replaced by scheduled starts when known, searches filter at the row arrival, IDs used by other slots are excluded, and pool merges preserve earlier candidates. **Reverified read-only 2026-08-01:** helper suite 8/8; three slot-target browser cases 3/3; empty/weather/geocode recovery cases 11/11. Current `page.tsx` still uses slot arrival, stable numeric slots, occupied-ID exclusion, and pool merge. The newer geocode mode did not break `empty` or `weather-gate`. |
| M6 | Confirmed — fixed | Provider requests had no deadline, and the late-night Places expansion used all-or-nothing `Promise.all`. | `app/api/_shared/provider.ts`; Groq, Places, Routes, Weather, and Redis callers; `searchPlaces.ts`; provider/search tests | Timeout and non-JSON provider cases plus a late-night partial-sibling search case. | Yes — restoring all-or-nothing late-night expansion fails the new sibling-survival case (14/15); restored result is 15/15. | SELF | — | Provider failures map to stable 502/504 responses; Places category isolation and keep-on-missing behavior remain intact. **Additional reverification 2026-08-01:** the model-fallback stream reshaped provider failures but retained the deadline/public mapping boundary; focused HTTP/provider coverage is 12/12 and fallback coverage 19/19. Only 429/provider 5xx advance a model chain. |
| M7 | Confirmed — fixed | The page used raw `fetch().json()` calls with no client deadline or response validation, several rejection paths skipped busy-state cleanup, independent busy flags allowed cross-action races, a new plan could retain stale weather, and lost mutation/follow-up responses could leave the UI silently stale. | `app/lib/clientFetch.ts`; `app/lib/clientPayloads.ts`; `app/page.tsx`; `e2e/client-resilience.spec.ts`; mock-E2E harness/config | 9 fetch-boundary cases, 7 payload-parser cases, and 17 browser scenarios cover every planning stage, aborts, non-JSON/invalid and nested bodies, recovery rejection, stale weather, cross-action races, reroute/swap rejection, committed-but-lost responses, follow-up 500s, and busy cleanup. | Yes — removing swap cleanup from `finally` makes the rejection scenario fail with the control still busy (1/1); restored result is green. | SELF | — | One typed boundary covers request, body, and validation deadlines. A ref-backed operation lock prevents same-tick and cross-action overlap. Weather is fail-soft but never stale; ambiguous mutation results read back the saved plan, and failed read-backs warn that the display may be out of date. **Additional read-only reverification 2026-08-01:** planner, geocode, recovery, and auth additions materially changed `page.tsx`, but client fetch 9/9, client payload 8/8, and all 17 client-resilience browser cases in the baseline run passed. Same mechanism survives. |
| M8 | Confirmed — fixed | The module-level Maps promise permanently cached its first rejection, both map effects awaited it without a catch, no non-Maps projection existed, and the earlier fix lacked browser proof for provider authentication failure, explicit retry, and remount after rejection. | `app/ItineraryMap.tsx`; `app/lib/retryableLoader.ts`; `app/globals.css`; `app/test-harness/maps/*`; `e2e/maps-resilience.spec.ts`; mock Playwright harness/docs | 3 loader unit cases, the offline strip↔fallback-pin smoke, and 3 real-component browser scenarios covering provider-signalled invalid key, blocked-script retry success, and remount recovery. | Yes — retaining the rejected loader promise makes the retry/remount browser scenarios fail (2/2); restored result is green. | SELF | — | The production component catches provider failures, evicts rejected attempts, offers two bounded retries, and preserves usable deterministic fallback pins. The mock-only harness 404s outside `E2E_MOCK`, intercepts the provider script locally, and proves auth and transport recovery without a real key or external traffic. |
| M9 | Confirmed — fixed | Routes trusted loosely typed JSON, unbounded arrays/text, invalid coordinates/times/zones, and incomplete model envelopes. | `app/api/_shared/schemas.ts`; all public route handlers; parse/select/swap provider validation; boundary and route tests | Invalid coordinate, dwell, timezone, oversized prompt, malformed model output, and exact parse-schema cases. | No | SELF | Persisted-itinerary version/CAS validation is completed with Batch C. | Strict schemas bound collection sizes and text, require finite/ranged values and valid ISO/IANA data, and reject unknown parse-model keys. **Additional reverification 2026-08-01:** the planner/model envelopes changed materially, but focused parse 14/14, HTTP/schema 12/12, client payload 8/8, and select 28/28 all pass. Strict response boundaries remain; the new raw-prompt semantic gaps are tracked separately as `H8/M3-F1` and `M1-F1`. |
| M10 | Confirmed — fixed | `/api/geocode` used generic Places Text Search, accepted the first result without checking place type/components, and gave the client no ambiguity contract. | `app/api/geocode/*`; shared provider/mock seam; client payload/parser and `page.tsx`; env/deployment/current docs | 9 geocode core/route cases, 1 client ambiguity contract case, provider-shaped fixture coverage, and 2 browser flows for city/address choice and city-context threading. | Yes — restoring first-result selection fails both ambiguous-city and multiple-valid-address cases (7/9); restored result is 9/9. | SELF | Enable Geocoding API, set/API-restrict `GOOGLE_GEOCODING_API_KEY`, and live-check representative cities/addresses before deployment. | Uses the purpose-built Geocoding API v3. City results require locality types/components; address requests carry viewport bias plus country restriction and are checked against locality/region/country. Multiple valid results enter the existing recovery component's `geocode` mode with formatted-address choices; no venue search runs before the choice. **Additional reverification 2026-08-01:** geocode remains first in the client pipeline; focused core/route coverage is 9/9 and both ambiguity browser flows pass alongside all older recovery modes. Same fix survives. |
| M11 | Confirmed — fixed | Empty pools were removed before model validation and the remaining slots were renumbered densely; valid model answers using original slot IDs appeared missing and triggered unnecessary correction calls. | `app/api/select/selectVenues.ts`; select route/mock completion; selection tests | First/middle empty slots, two separated live slots, original slot IDs, exact one-call valid responses, and post-retry validation. | Yes — restoring dense renumbering makes the partial-live-slot call-count case perform a redundant correction; restored focused batch result is 147/147. | SELF | — | Live slots keep their original numbers end to end, and correction output is validated again before any deterministic fallback is assembled. **Reverified 2026-08-01:** select 28/28 and model fallback 19/19. The partial-live-slot case still preserves original slot IDs, a valid first response still costs one selection completion, and correction output is revalidated on the same model before deterministic fallback. |
| M12 | Confirmed — fixed | Routes failure fell back to optimistic crow-flies walking time; every leg requested TRANSIT even when the short-hop rule could only present WALK; the UI omitted Google's walking caution and mapped unknown estimates as confident solid straight lines. | `app/api/schedule/travel.ts`; `ItineraryStrip.tsx`; `ItineraryMap.tsx`; `mapRoutePolicy.ts`; CSS; travel/unit/map/browser tests | Irregular-geography fallback, exact short-hop provider modes/call count, map display policy, real-component unknown-line omission, and visible/readable accessible warning coverage. | Yes — restoring optimistic fallback plus unconditional TRANSIT fails exactly the 2 new travel cases; hiding the caution fails its browser case; restoring unknown-as-walk line rendering fails the map browser case. | SELF | — | Fallback uses 1.35× detour plus a separate 20% uncertainty margin (minimum 5 min) and remains `unknown`; <250 m haversine hops call WALK only; unknown legs draw no route; every displayed WALK leg carries a normal-opacity 11 px caution. |
| M13 | Pending product-contract investigation | — | — | — | — | — | Define ownership/sharing semantics before user-visible enforcement. | **Recommendation only; no implementation. Already merged:** current `main` has optional Stage 1A client Google sign-in; guests retain the full app, and server itinerary APIs neither verify tokens nor bind/enforce owners. **Active branch evidence:** local/remote inspection found no Stage 1B branch; `origin/authentication` contains Stage 1A only. **Proposed contract:** keep anonymous use supported; server-verify Firebase tokens; bind newly created signed-in plans to the verified UID; migrate only anonymous plans proven to belong to the current client/session when login occurs; require owner authorization before reads/mutations and combine that check with existing CAS/version preconditions; do not treat a UUID as authority. Omit sharing from the first owner-enforcement release unless explicitly chosen; if included, use a separate revocable, expiring, read-only scoped token, with invalid/expired tokens denied without disclosing plan existence. Add explicit owner deletion and history/archive semantics; minimize persisted home labels; migrate ownership with CAS; keep legacy unowned plans backward-compatible until a deliberate migration/expiry policy exists. **Unresolved product decisions:** whether sharing ships initially, how anonymous-session proof/migration works, anonymous and legacy retention, delete versus archive/history retention, home-label granularity, and legacy-unowned sunset. UUID leakage currently exposes private itinerary details to anyone who obtains it and must never become the ownership/share contract. |
| M14 | Confirmed — fixed | Routes and engines logged or returned raw exception/provider/model detail without a correlation-safe error contract. | `app/api/_shared/http.ts`; provider callers; parse, search, schedule, weather, swap, and reroute logging sites | Unexpected-error coverage asserts a request ID while excluding supplied secret text from both logs and responses. | No | SELF | Define and operate the production log retention, access, redaction, and deletion policy; monitor by request ID. | **Reconfirmed 2026-08-01:** public failures remain sparse/stable and carry `x-request-id` plus a response request ID. Structured failure logs contain correlation/scope/status/code only; prompts, refinements, provider bodies, arbitrary errors, tokens, keys, locations, and environment values are excluded. Model fallback adds internal retry classification but does not change the public error contract. Focused shared HTTP/provider result: 12/12; model-fallback result: 19/19. |
| M15 | Recovered — fixed and guarded | On the original narrow-phone landing page, the prompt stayed horizontal: around 375 px the main field was about 68 px wide and the location fields about 42 px, making ordinary text entry unusable. Vertical dividers and an undersized action compounded the problem. | No implementation in this session. Current evidence: `app/globals.css`; read-only landing structure in `app/page.tsx`; `e2e/mobile.spec.ts`. | Existing browser matrix verifies 320, 375, 390, and 768 px: all three inputs editable with ≥88 px useful width and ≥44 px height; narrow fields stack without overlap; dividers become horizontal; primary planned-stage controls and map chips retain ≥44×44 px targets and viewport reachability. | Not applicable — investigation/documentation-only change. | SELF | Keep the four-width browser matrix in the aggregate runner; no fenced-file implementation is required. | Fixed and guarded by the later responsive redesign. At ≤560 px `.prompt` becomes a column, each section is 100% width, the separator is 1 px horizontal, and the submit button remains 52×52 px. Focused browser result: 4/4. |
| M16 | Recovered — fixed and guarded for the original concerns | The original card used `role="button"` around an input/button; loading/error and disruption updates lacked consistent live semantics; the map label was hard-coded to Ossington; keyboard focus after swap/reroute lacked coverage. | No implementation in this session. Current evidence: `app/ItineraryStrip.tsx`; `app/ItineraryMap.tsx`; read-only `app/page.tsx`; `e2e/accessibility.spec.ts`; `axe-core`. | Existing tests execute axe on empty/planned/expanded-swap surfaces; prove the native selection button contains no interactive descendant and the swap form is its sibling; verify status/alert semantics, accessible names, keyboard selection state, visible focus, and focus restoration for successful/refused swap and reroute. | Not applicable — investigation/documentation-only change. | SELF | Continue running the accessibility file; this closes the original candidates but is not a claim of exhaustive WCAG certification. | Fixed and guarded. Map `aria-label` now incorporates the resolved home label, and home-leg display uses `originDisplayLabel`; no hard-coded Ossington map label remains. Focused browser result: 6/6, including a real axe execution. |
| M17 | Confirmed in part — safe efficiency fix; proposed cross-request cache/enrichment rejected | The field mask and call envelope were unmeasured, and overlapping late-night categories could issue identical queries (including `late night late night …`). However every expensive field has a current deterministic/UI consumer; full Places caching conflicts with provider policy and hours freshness, while discovery plus Details adds calls without a fact-safe shortlist. | `app/api/places/search/searchPlaces.ts`; its tests; `README.md`; `DEPLOY.md`; `CLAUDE.md` | 7 cases cover exact field consumers/mask, one-stage baseline, overlap dedupe, hours freshness, city/category/mode isolation, failed-call eviction, and fixed general/day/late ceilings with pre-upstream rejection. | Yes — bypassing request-local dedupe makes the overlap case issue the shared query twice (21/22); restored result is 22/22. | SELF | Monitor real billing/latency and keep Google quotas/alerts current; revisit only if provider policy/pricing or product field needs change. | **Reconfirmed 2026-08-01: 22/22 focused tests.** Ordinary category 1→1, eight daytime 8→8, eight late-night max 16→16, general 5→5; overlapping `[bar, late night bar]` 4→2. Identical normalized query+type promises dedupe only within one `searchPools` invocation; failed work is evicted and later attempts refetch hours. The prior rejection of a cross-request Places TTL cache remains deliberate because provider policy and opening-hours freshness have not changed, and there is still no fact-complete safe shortlist for discovery plus Details. |
| M18 | Confirmed — fixed; warning cleanup deferred | `next lint` was interactive, `tsx` was undeclared, and unit suites had no aggregate runner. | `package.json`; `package-lock.json`; `eslint.config.mjs`; `scripts/run-unit-tests.ts`; `.gitignore`; two lint-only source cleanups | Aggregate runner executed all 251 existing unit tests; the full check also executed 41 mock E2E tests. | No | SELF | Defer lint/dead-code cleanup until after Stage 1B because live warnings include fenced `app/page.tsx`. Start that batch from a fresh `npx eslint .`; separate configuration intent from deletion; make a deliberate ESLint-policy decision for intentionally underscore-prefixed test parameters and do not delete signature-documenting parameters merely to silence warnings. | **Reconfirmed 2026-08-01:** deterministic `npm ci` passed; current lint is 0 errors/11 warnings; typecheck, all 459 unit tests across 36 suites, and production build pass. The aggregate runner is functional and reaches mock E2E; its current failure is the separate clock-dependent M19 test defect, not toolchain setup. No remembered warning count was used. |
| M19 | Confirmed — deterministic mock-E2E clock regression; implementation deferred | The stated-window browser cases use the real wall clock. When run at 20:26 Toronto, `5-9pm` is correctly treated as an underway window and its start is clamped to now; after the 16-minute home leg, the first 105-minute stop ends after the 21:30 tolerance, so `keep:0` is correct but the test still expects the earlier full-window plan. The `7-9pm` case fails for the same reason. | Tracker only in this session; affected tests/fixture clock seam are `e2e/scenarios.spec.ts`, `app/api/_mock/fixtures.ts`, and the browser `nowISO` path in `app/page.tsx` (read-only). | None — investigation/documentation-only change. Both focused failures were reproduced and their Playwright network traces inspected. | Not applicable — investigation/documentation-only change. | SELF | Make the two window scenarios clock-independent using an existing supported time seam or a deliberate fixed browser clock; do not weaken the correct underway-window behavior. | Baseline result was 71/73. Trace evidence: parse received `nowISO: 2026-08-02T00:26:43Z`; the plan carried start `20:26` and end `21:00` local; travel added 16 minutes before a 105-minute dinner. This is a test determinism defect, not a reason to change production window arithmetic. |
| L1 | Confirmed — fixed | Malformed or mixed opening-hour periods were skipped and a resulting non-match was returned as definitely closed, violating keep-on-missing-data. | `app/api/places/search/hours.ts`; hours/filter tests | Missing close, malformed values, mixed valid/malformed periods, valid non-match, overnight, 24/7, and tri-state instant checks. | Yes — treating malformed non-matches as false fails the inconclusive-hours regression; restored focused batch result is 147/147. | SELF | — | A valid matching period proves open; all-valid non-match proves closed; any malformed data that prevents a conclusion returns `null` and keeps the venue. |
| L2 | Recovered — fixed and guarded; production policy is explicit | Time-travel and disruption controls were originally visible to every production visitor. | No implementation in this session. Current evidence: `app/lib/devControls.ts`; its unit test; read-only `app/page.tsx`; `.env.example`; `README.md`; `DEPLOY.md`. | Eight policy cases cover development/test defaults plus production absent, blank, `false`, `TRUE`, `1`, and exact `true`. | Not applicable — investigation/documentation-only change. | SELF | Leave `NEXT_PUBLIC_ENABLE_DEV_CONTROLS` unset/`false` for normal production; set exact `true` and rebuild only for an intentional demo. | Production now hides the controls unless the build-time flag is exactly `true`; development/test keep them visible intentionally. `.env.example` defaults to `false`. Focused result: 8/8. |
| L3 | Confirmed — fixed | Runtime CSS imported Fraunces and Space Grotesk from Google Fonts. | `app/layout.tsx`; `app/globals.css`; `package.json`; `package-lock.json`; mock Playwright harness/docs | The complete mock E2E suite runs with every non-local browser request blocked. | No | SELF | — | Uses OFL-1.1 `@fontsource-variable` packages served from the application bundle; the production build and hydration smoke pass. |
| D1 | Recovered — candidates verified; no deletion performed | Original candidates were the root `index.html` prototype, an empty untracked `New` folder, dead `.ecard*`, `.wherebar`, `.swapbar*`, and `.leglab--dim` CSS, unread `parsedObj` / `travelLegs` state, an unused `useEffect` import, unused `MapStop.reason` / `blockedReason`, and the unused `t` parameter in `nextItineraryStatus`. | No implementation in this session. Read-only search across the current tree and history; live ESLint output. | No tests added; this is an inventory, not a behavior change. | Not applicable — investigation/documentation-only change. | SELF | Perform cleanup only after Stage 1B, starting from fresh lint/search evidence. Decide whether the tracked root prototype has archival value before deleting it. | Current status: root `index.html` still exists, is tracked from the initial commit, and is not part of the Next runtime (runtime-dead, possible archival artifact); `New` is gone; `.ecard*`, `.wherebar`, `.swapbar*` (including mobile rule), and `.leglab--dim` remain CSS-only/dead; `parsedObj` and `travelLegs` remain setter-only and produce live lint warnings; the unused `useEffect` import is removed; `MapStop.reason` and `MapStop.blockedReason` remain unused by `ItineraryMap` (strip reason uses a separate `StripStop` path); `nextItineraryStatus` still has an unused `t` parameter and live lint warning. Nothing was deleted. |
| D2 | Completed — current-state docs reconciled | README/DEPLOY had stale dependency versions, install/test commands, recovery modes, deployment root, timezone rationale, provider/model behavior, browser-visible configuration, auth claims, and operational limits. | `README.md`; `DEPLOY.md`; this tracker note | No tests added. Commands documented in the new reconciliation were smoke-checked: `npm ci`, lint, typecheck, aggregate unit, build, `npm run check`, mock E2E, `npm audit`, and production-only audit. | Not applicable — documentation-only change. | `38d5e15` | Keep historical `DEVLOG.md`, the 2026-07-18 report, and already-reconciled canonical instructions unchanged unless a concrete current contradiction appears. Live provider/auth/dashboard checks remain owner work. | README/DEPLOY now describe Node ≥20.9, Next 16/React 19, geocode-first per-plan-zone pipeline, three recovery modes, Maps fallback, mock isolation/port 3100, live port 3000 ownership, model chains, optional Stage 1A auth, current keys, Redis behavior, per-process rate-limit limits, and external quota/key restrictions. Stage 1B is explicitly not claimed. |

## Queued work — transit detail and refresh (do not start before Stage 1B merge/audit)

This separate product batch was queued before the 2026-08-01 resume and was not implemented here. It may overlap files owned by Stage 1B, so it remains blocked on that merge and audit review.

- Preserve provider transit steps as ordered structured data. Render walk, board, ride, exit, transfer, wait, and final-walk steps chronologically; keep every route number, headsign, stop, platform, boarding time, and arrival time attached to the correct ride; prevent overlapping or ambiguously stacked route bubbles.
- Distinguish scheduled times from genuine expected/live times. Verify exactly what Google Routes supplies before claiming realtime behavior, never invent live delay data, and use a bounded, cost-conscious refresh strategy only if suitable realtime fields exist.
- After a real delay, recalculate downstream deterministic times, detect impossible transfers, and invoke the existing disruption/reroute flow when a connection breaks while preserving locked stops and `floorTime()`.
- Remain usable when refresh fails. Add regression, accessibility, and narrow-mobile coverage.
- Reuse the production travel/scheduling cores. Do not parse display strings in React and do not create a second UI-only transit engine.
