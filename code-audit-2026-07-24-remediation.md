# Code audit remediation — 2026-07-24

This is the live tracker for the 2026-07-24 full-project audit. Every finding is verified
against the current repository before implementation. Historical `DEVLOG.md` entries and the
2026-07-18 audit are evidence, not assumed current truth.

## Baseline

- Branch: `main`
- Starting commit: `6f36584ab29e9eb10d69a3556a13db9c673e3ee9`
- Starting untracked paths: `AGENTS.md` only (user-owned, relevant, preserved)
- Node: `v24.16.0`
- npm: `11.13.0`
- TypeScript: pass (`npx tsc --noEmit`)
- Production build: pass (Next.js 14.2.35)
- Unit tests: 251/251 across 18 suites
- Mock E2E: 41/41 on port 3100
- Lint: unusable; `next lint` opened an interactive configuration prompt
- Audit: failed with 2 high-severity dependency findings (Next.js and PostCSS)
- Port 3000: untouched

## Finding tracker

`SELF` in the commit column means the row was updated in the same commit as the change; exact
hashes are reconciled after the commit exists.

| Finding ID | Verified status | Root cause | Files changed | Tests added | Revert-run performed | Commit hash | Remaining manual work | Notes or deviations |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| H1 | Confirmed — source mitigated; external global limit required | Every public route accepted unbounded request bodies and unlimited bursts before doing provider or persistence work. | `app/api/_shared/http.ts`; all public route handlers; `DEPLOY.md` | 4 boundary cases cover declared/actual oversize rejection and rate-limit ordering ahead of parse-provider work. | No | SELF | Configure the documented shared edge or Redis-backed limiter in production; the in-process limiter is defense in depth, not a global serverless quota. | Limits are applied before body parsing, provider calls, and itinerary loads; 429 responses include `Retry-After`. |
| H2 | Confirmed — fixed | Next 14.2.35 and its bundled PostCSS/Sharp versions were covered by current high-severity advisories. | `package.json`; `package-lock.json`; `next.config.mjs`; `tsconfig.json`; `next-env.d.ts`; three dynamic itinerary routes | Existing 251 unit and 41 mock E2E tests exercised after the migration. | No | SELF | Full audit still reports dev-only transitive `brace-expansion`/`minimatch` advisories under ESLint; production audit is clean. | Upgraded to Next 16.2.11/React 19.2.8, migrated async route params, and pinned patched PostCSS/Sharp through Next-scoped overrides. |
| H3 | Confirmed — fixed | Redis and memory routes performed mutable load/modify/save cycles with no version check; GET lock ratcheting could overwrite a newer swap/reroute and every Redis save reset the TTL. | `app/api/itinerary/store.ts`; create/read/swap/reroute routes; client mutation bodies; shared version schema | 9 new cases: two swaps, swap versus reroute, lock ratchet versus swap, stale writer 409, failed-CAS isolation, no-op version stability, route ETag/conflict ordering, and Redis atomic TTL preservation. | Yes — removing the in-memory version comparison fails 4/6 CAS cases with lost edits, missing retries, and no stale rejection; restored result is 6/6. | SELF | — | New plans start at version 1; Redis CAS is one Lua `EVAL` using `SET ... KEEPTTL`; memory mode clones and CASes; legacy unversioned Redis records normalize to version 1; explicit stale clients fail before provider work while safe versionless/status operations retry at most three times. |
| H4 | Confirmed — fixed | Reroute compacted selections with IDs/locations for routing, then scheduled and wrote the uncompacted slot list; missing/malformed selections could silently shorten the route or attach an A→C leg to A→B, and the engine mutated the caller before proving completeness. | `app/api/itinerary/reroute.ts`; reroute route; atomic/route tests | Empty/missing selections, missing middle coordinates, A→B→C alignment, provider failure, partial-failure byte identity, and failed-route version stability. | Yes — accepting a missing location makes the middle-coordinate case alone fail (11/12) by committing the malformed chain; restored result is 12/12. | SELF | — | The engine clones first, requires exactly one valid unique in-pool ID and coordinate pair per affected slot, rejects gaps/compaction, verifies schedule/arrival agreement, and copies back once only after the complete chain passes. |
| H5 | Confirmed — fixed | Every replacement was filtered at one floor/anchor instant and every downstream route request reused that same departure, so later openings, closings, weather, and transit timing were judged at the wrong time. | `app/api/itinerary/reroute.ts`; arrival-aware reroute tests | Accumulated departure times, later opening, closes-before-arrival replacement, slot-varying weather, longer-leg closing crossover, bounded exhaustion, and one-fetch pool/weather assertions. | Yes — forcing all validation back to the anchor fails 6/12 cases, including later opening, closing, weather, long-leg, exhaustion, and atomic-failure scenarios; restored result is 12/12. | SELF | — | Pools and forecast are fetched once; up to five stabilization passes route sequentially, validate each candidate through the shared objective filter at its settled arrival, exclude invalid IDs, and reroute the full tentative chain. |
| H6 | Confirmed — fixed | Venue swaps wrote the target before proving its new inbound leg could reach the held start, and every swap treated the first locked tail stop as a successful stopping point even when the new prefix overlapped it. | `app/api/itinerary/swap.ts`; swap route; legacy and atomic swap tests | Farther fixed-slot venue, valid moved target, locked collision, inconsistent/reachable locked boundaries, production-mock seam, and failed-swap version/no-mutation coverage. | Yes — restoring the silent missing-tail `continue` made the missing-middle case fail by reporting a successful partial proposal (10/11 at the time of the revert); the restored current suite is 12/12. | SELF | — | Venue swaps keep the complete existing slot and now refuse an unreachable replacement; time swaps may move the target; duration swaps keep its start. Every path routes the anchor inbound before write-back and proves any locked boundary is reachable without overlap. |
| H7 | Confirmed — fixed | `resettleTail` returned success after `break` on missing coordinates or a locked boundary, checked only hours before shifting, fetched weather repeatedly, and did not revalidate an adapted venue after its new inbound leg changed arrival. | `app/api/itinerary/swap.ts`; swap route; `swap.atomic.test.ts`; corrected locked-boundary expectations in `swap.test.ts` | Shifted closing/weather cases, missing middle location, replacement closing crossover, one-weather-fetch assertion, route failure byte identity, and complete reachable-boundary success. | Yes — the H6/H7 shared revert-run above directly resurrected the partial-tail success bug and was caught by the atomic suite. | SELF | — | Every swap is built on a clone with one forecast snapshot. The tail is routed sequentially; existing and adapted stops pass the shared objective filter plus the availability seam at final arrival; replacement-route changes trigger another validation; only a complete proposal is copied back, while refusal is a persistence no-op. |
| H8 | Confirmed — fixed | The scheduler understood clocks and a special `tomorrow` branch but discarded weekday/date qualifiers, relied on permissive `Date` normalization, and had no deterministic DST policy. | `app/api/schedule/schedule.ts`; `app/lib/zoneTime.ts`; schedule/parse/mock tests; `README.md` | Bare/next weekdays, ISO/named dates, date-plus-clock, invalid dates, DST gap/overlap, and mock parse preservation. | Yes — removing bare-weekday resolution fails the calendar regression; restored focused batch result is 147/147. | SELF | — | Calendar math stays in `zoneTime.ts`; bare weekdays use the documented nearest-future rule, `next` is strict when today matches, DST gaps fail, and overlaps select the earlier instant. |
| H9 | Confirmed — fixed | The selection prompt allowed general knowledge and prose/name inference as factual evidence, accepted unrelated unmet constraints, and could fall back to an unverified high-rated venue. | `app/lib/constraints.ts`; `app/api/select/selectVenues.ts`; select route/mock seam; fixture/filter/fail-loud tests | Normalization/inert-text cases, unrelated unmet values, unknown and valid accessibility, malformed retries, injection strings, multi-constraint reasons, and structured vegetarian E2E. | Yes — disabling code-side constraint enforcement makes the dedicated selection regressions fail; restored focused batch result is 147/147. | SELF | — | Only explicit provider booleans/accessibility fields become evidence. Unknown stays unknown; every selected ID and unmet constraint is revalidated after retry; constrained fallback uses evidenced candidates only. |
| M1 | Confirmed — fixed | `stop_count` was parsed but never converted into requested slots, so exact counts could silently collapse to one stop or bypass a missing-kind decision. | `app/lib/planSlots.ts`; `app/lib/clarify.ts`; parse/schema/schedule/select/client/mock code; unit and E2E tests | Three coffee shops, exactly three places, ambiguous dinner/drinks distribution, repeated categories, invalid counts, mandatory clarification, and three distinct strip/pin stops. | Yes — removing single-category expansion fails the repeated-count regression; restored focused batch result is 147/147. | SELF | — | Counts are bounded integers. One category expands exactly; multiple-category ambiguity asks for an explicit distribution; blank Go/Skip cannot bypass a counted request that still lacks a kind. |
| M2 | Confirmed — fixed | The objective filter removed an ID globally after it survived an earlier category, destroying feasible cross-category matchings before selection. The deterministic fallback also let later duplicate slots steal earlier top-ranked picks. | `app/api/places/search/filter.ts`; `app/api/select/selectVenues.ts`; filter/select/mock/browser tests | Dinner `[A,B]` plus drinks `[A]`, three overlapping categories, shared IDs across pools, repeated categories, narrowed shortages, and stable rating order. | Yes — restoring global pre-dedup fails the shared-ID filter/assignment cases; restored focused batch result is 147/147. | SELF | — | Deduplication is now per pool. Final IDs are globally unique through validated model output or maximum-cardinality augmenting assignment; free candidates are preferred before displacement to preserve request-order ranking. |
| M3 | Confirmed — fixed | Clock extraction accepted impossible meridiem/24-hour values (and signed lookalikes), malformed date-shaped text could be ignored, and plausibility passed when any later category allowed the hour. | `app/api/places/search/hours.ts`; `app/api/schedule/schedule.ts`; parse/schedule/hours tests | `13pm`, `00pm`, `24:00`, bad minutes, signed clocks, malformed dates, bare weekday/date 24-hour clocks, and first-category plausibility. | No — the strict parser and first-slot acceptance boundary are covered directly; no safe one-line revert represented both causes. | SELF | — | Clock parsing distinguishes absent from malformed input. Meridiem clocks require 1–12, bare clocks 0–23, minutes 0–59, and the first scheduled category owns plausibility. |
| M4 | Confirmed — fixed | Any budget containing a number was treated as “cheap,” so `under $300` and non-dollar amounts received the same hard Places cap as `under $20`. | `app/lib/budget.ts`; parse/filter/select/search/mock code; budget/filter/fixture tests | `$`, `$$`, cheap/budget language, `$20`, `$300`, prefix/suffix currencies, thousands separators, conflicting currencies, and missing price. | Yes — restoring a hard cheap cap for numeric maxima fails the numeric-budget filter case; restored focused batch result is 147/147. | SELF | — | Relative language and dollar-symbol levels retain defensible Places caps. Numeric maxima preserve amount/currency as ranking context only; missing/ambiguous price data remains eligible. |
| M5 | Pending verification | — | — | — | — | — | — | — |
| M6 | Confirmed — fixed | Provider requests had no deadline, and the late-night Places expansion used all-or-nothing `Promise.all`. | `app/api/_shared/provider.ts`; Groq, Places, Routes, Weather, and Redis callers; `searchPlaces.ts`; provider/search tests | Timeout and non-JSON provider cases plus a late-night partial-sibling search case. | Yes — restoring all-or-nothing late-night expansion fails the new sibling-survival case (14/15); restored result is 15/15. | SELF | — | Provider failures map to stable 502/504 responses; Places category isolation and keep-on-missing behavior remain intact. |
| M7 | Pending verification | — | — | — | — | — | — | — |
| M8 | Confirmed — partially fixed | The module-level Maps promise permanently cached its first rejection, both map effects awaited it without a catch, and no non-Maps projection existed. | `app/ItineraryMap.tsx`; `app/lib/retryableLoader.ts`; `app/globals.css`; mock Playwright harness/docs | 3 loader unit cases; existing offline smoke now pins the accessible fallback and strip↔pin agreement | Yes — removing rejection eviction makes the recovery case fail (2/3); restored result is 3/3. | SELF | Batch G still needs invalid-key, retry-success, and remount browser scenarios alongside the shared client-fetch work. | The production component now catches failures, evicts rejected loads, offers two bounded retries, and keeps pins usable on a deterministic fallback projection. Mock E2E blocks all non-local browser requests. |
| M9 | Confirmed — fixed | Routes trusted loosely typed JSON, unbounded arrays/text, invalid coordinates/times/zones, and incomplete model envelopes. | `app/api/_shared/schemas.ts`; all public route handlers; parse/select/swap provider validation; boundary and route tests | Invalid coordinate, dwell, timezone, oversized prompt, malformed model output, and exact parse-schema cases. | No | SELF | Persisted-itinerary version/CAS validation is completed with Batch C. | Strict schemas bound collection sizes and text, require finite/ranged values and valid ISO/IANA data, and reject unknown parse-model keys. |
| M10 | Pending verification | — | — | — | — | — | — | — |
| M11 | Confirmed — fixed | Empty pools were removed before model validation and the remaining slots were renumbered densely; valid model answers using original slot IDs appeared missing and triggered unnecessary correction calls. | `app/api/select/selectVenues.ts`; select route/mock completion; selection tests | First/middle empty slots, two separated live slots, original slot IDs, exact one-call valid responses, and post-retry validation. | Yes — restoring dense renumbering makes the partial-live-slot call-count case perform a redundant correction; restored focused batch result is 147/147. | SELF | — | Live slots keep their original numbers end to end, and correction output is validated again before any deterministic fallback is assembled. |
| M12 | Pending verification | — | — | — | — | — | — | — |
| M13 | Pending product-contract investigation | — | — | — | — | — | Define ownership/sharing semantics before user-visible enforcement. | Does not block unrelated batches. |
| M14 | Confirmed — fixed | Routes and engines logged or returned raw exception/provider/model detail without a correlation-safe error contract. | `app/api/_shared/http.ts`; provider callers; parse, search, schedule, weather, swap, and reroute logging sites | Unexpected-error coverage asserts a request ID while excluding supplied secret text from both logs and responses. | No | SELF | Apply the documented production retention/redaction policy and monitor by request ID. | Public failures are sparse and stable; structured logs carry correlation IDs and omit prompts, refinements, upstream bodies, keys, and environment values. |
| M15 | Pending verification | — | — | — | — | — | — | — |
| M16 | Pending verification | — | — | — | — | — | — | — |
| M17 | Pending verification | — | — | — | — | — | — | — |
| M18 | Confirmed — fixed | `next lint` was interactive, `tsx` was undeclared, and unit suites had no aggregate runner. | `package.json`; `package-lock.json`; `eslint.config.mjs`; `scripts/run-unit-tests.ts`; `.gitignore`; two lint-only source cleanups | Aggregate runner executed all 251 existing unit tests; the full check also executed 41 mock E2E tests. | No | SELF | Batch J will remove the remaining 15 lint warnings and then enforce a zero-warning lint gate. | A normal clean `npm ci` succeeded, followed by lint, typecheck, unit, build, and mock E2E with exit 0. |
| L1 | Confirmed — fixed | Malformed or mixed opening-hour periods were skipped and a resulting non-match was returned as definitely closed, violating keep-on-missing-data. | `app/api/places/search/hours.ts`; hours/filter tests | Missing close, malformed values, mixed valid/malformed periods, valid non-match, overnight, 24/7, and tri-state instant checks. | Yes — treating malformed non-matches as false fails the inconclusive-hours regression; restored focused batch result is 147/147. | SELF | — | A valid matching period proves open; all-valid non-match proves closed; any malformed data that prevents a conclusion returns `null` and keeps the venue. |
| L2 | Pending verification | — | — | — | — | — | — | — |
| L3 | Confirmed — fixed | Runtime CSS imported Fraunces and Space Grotesk from Google Fonts. | `app/layout.tsx`; `app/globals.css`; `package.json`; `package-lock.json`; mock Playwright harness/docs | The complete mock E2E suite runs with every non-local browser request blocked. | No | SELF | — | Uses OFL-1.1 `@fontsource-variable` packages served from the application bundle; the production build and hydration smoke pass. |
| D1 | Pending verification | — | — | — | — | — | — | — |
| D2 | Confirmed | Current docs still describe old test commands, dependency versions, dev controls, and provider behavior. | Pending | Command smoke checks pending | No | Pending | Final reconciliation after behavior batches. | Historical entries will not be rewritten. |
